/**
 * Created by xyz on 20-Mar-17.
 */
public class numerepana0 {
    public static void main(String[] args) {
        int x,max=0;

        do {
            x=SkeletonJava.readIntConsole(" x = " );
            if (x>max)

            max=x;
                    }
        while ( x!=0);
        System.out.println( " maxinul este " +max);
                        }
}
